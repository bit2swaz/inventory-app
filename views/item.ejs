<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <div class="container">
    <header>
      <h1><%= item.name %></h1>
    </header>
    
    <main>
      <div class="item-details">
        <div class="item-info">
          <p class="description"><%= item.description %></p>
          <p class="category">Category: <a href="/categories/<%= item.category_id %>"><%= item.category_name %></a></p>
          <p class="price">Price: $<%= parseFloat(item.price).toFixed(2) %></p>
          <p class="stock">In Stock: <%= item.stock %></p>
          <p class="added">Added: <%= new Date(item.added_at).toLocaleDateString() %></p>
        </div>
      </div>
      
      <div class="navigation">
        <a href="/items" class="btn">Back to Items</a>
        <a href="/categories/<%= item.category_id %>" class="btn">View Category</a>
        <a href="/" class="btn">Back to Home</a>
      </div>
      
      <div class="theme-toggle">
        <button id="theme-switch" class="btn">Toggle Dark Mode</button>
      </div>
    </main>
    
    <footer>
      <p>made with ❤ by <a href="https://github.com/bit2swaz" target="_blank">bit2swaz</a></p>
    </footer>
  </div>
  
  <script src="/js/theme.js"></script>
</body>
</html> 