<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <div class="container">
    <header>
      <h1><%= item.name %></h1>
    </header>
    
    <main>
      <div class="item-details">
        <% if (isAdmin) { %>
          <div class="admin-actions">
            <a href="/items/<%= item.id %>/edit" class="btn btn-primary">Edit Item</a>
            <a href="/items/<%= item.id %>/confirm-delete" class="btn btn-danger">Delete Item</a>
            <a href="/admin/logout" class="btn">Logout</a>
          </div>
        <% } %>
        
        <h2>Details</h2>
        <p class="description"><%= item.description %></p>
        
        <div class="item-meta">
          <p class="price">Price: $<%= parseFloat(item.price).toFixed(2) %></p>
          <p class="stock">Stock: <%= item.stock %> units</p>
          <p class="category">Category: <a href="/categories/<%= item.category_id %>"><%= item.category_name %></a></p>
          <p class="added">Added on: <%= new Date(item.added_at).toLocaleDateString() %></p>
        </div>
      </div>
      
      <div class="navigation">
        <a href="/categories/<%= item.category_id %>" class="btn">Back to Category</a>
        <a href="/items" class="btn">All Items</a>
        <a href="/" class="btn">Home</a>
      </div>
      
      <div class="theme-toggle">
        <button id="theme-switch" class="btn">Toggle Dark Mode</button>
      </div>
    </main>
    
    <footer>
      <p>made with ❤ by <a href="https://github.com/bit2swaz" target="_blank">bit2swaz</a></p>
    </footer>
  </div>
  
  <script src="/js/theme.js"></script>
</body>
</html> 